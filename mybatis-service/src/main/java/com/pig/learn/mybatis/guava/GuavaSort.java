package com.pig.learn.mybatis.guava;

public class GuavaSort {

//      1. //对结果排序规则，
//              2. // 1.按Id排序
//              3. // 2.对同一个id，按name排序
//              4. // 3.对同一个id，相同name，按优先级排序
//              5. List<Person> personList = new ArrayList<>();
//  6. Collections.sort(personList,new Comparator<Person>(){
//        7.     @Override
//        8.     public int compare(Person o1, Person o2) {
//            9.         return ComparisonChain.start()
//            10.                 .compare(o1.getId(),o2.getId())
//            11.                 .compare(o1.getname(),o2.getname())
//            12.                 .compare(o1.getLevel(),o2.getLevel()).result();
//            13.     }
//        14. });
}
